# Веб-приложение с Nginx и Docker

## 🚀 Как запустить проект
### Сборка контейнеров
```bash
docker-compose -p web-project build

### Запуск контейнеров
```bash
docker-compose -p web-project up -d

## ✅ Как проверить результат
curl http://localhost

### Ожидаемый ответ
Hello from Effective Mobile!

## 📋 Как работает схема (nginx → backend)
Когда пользователь обращается по адресу http://localhost, его запрос поступает на порт 80 хостовой машины. Этот порт проброшен в контейнер с Nginx. Nginx, выступая в роли обратного прокси-сервера, принимает запрос, анализирует свой конфигурационный файл и находит правило перенаправления. Согласно этому правилу, Nginx пересылает запрос на контейнер с backend-приложением по внутреннему адресу backend:8080 через изолированную Docker-сеть.